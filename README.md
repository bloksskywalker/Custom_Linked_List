# Custom Linked List
This is a simple program that is a custom implementation of an integer linked list. 

This program can be interacted with by entering mode 1, or automatic testing can be done by entering mode 2.

Be careful when doing any operation involving an index, as the program will crash if the index does not exist.
Use the find first index of operation first.

## Made for CS Class